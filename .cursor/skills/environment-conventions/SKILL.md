---
name: environment-conventions
description: Environment Conventions
---

# Overview

Use docker compose (not docker-compose) and do not include version: in compose files unless the repo already does. Use the repo’s existing .env.example pattern; do not create new .env files.